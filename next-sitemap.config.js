/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: process.env.SITE_URL || 'https://mstuart1.github.io/portfolio',
  generateRobotsTxt: true, // (optional)
  // ...other options
}